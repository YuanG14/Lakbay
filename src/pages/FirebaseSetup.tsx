import { Route, Settings2 } from 'lucide-react';
export default function FirebaseSetup(){return <div className="setup-page"><div className="auth-brand"><div className="brand-mark"><Route size={24}/></div><div><div className="brand-name">Lakbay</div><div className="brand-sub">Smart Trip Planner</div></div></div><div className="setup-card"><Settings2 size={32}/><span className="section-kicker">Firebase setup required</span><h1>Connect your Firebase project</h1><p>Lakbay is ready for Firebase, but project credentials are intentionally not included in the ZIP.</p><ol><li>Create a Firebase project and Web App.</li><li>Enable <strong>Email/Password</strong> in Authentication.</li><li>Create a <strong>Cloud Firestore</strong> database.</li><li>Copy <code>.env.example</code> to <code>.env</code> and paste your Firebase web config values.</li><li>Deploy the included <code>firestore.rules</code> rules and restart <code>npm run dev</code>.</li></ol><p className="setup-note">Lakbay shows this setup screen instead of crashing when environment variables are missing.</p></div></div>}
